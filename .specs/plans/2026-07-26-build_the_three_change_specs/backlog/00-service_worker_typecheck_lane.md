# Task 00 · service worker typecheck lane

**Plan:** [plan.md](../plan.md) · **Certificate:** [00-service_worker_typecheck_lane-certificate.md](00-service_worker_typecheck_lane-certificate.md)

**Implements:** none of the three change specs directly. It is the prerequisite the
plan owes tasks 13 and 22: [`.specs/development-guidelines.md`](../../../development-guidelines.md)
§Definition of done asks for a test at the right tier, and the boundary those two
tasks rewrite has no tier at all today. Targets one row of
[`10-build-release-deploy.md`](../../../10-build-release-deploy.md) §Recipes.
**Depends on:** none
**Produces:** `web/src/service-worker.ts` enters a tsc program for the first time,
so the positional `feed_page` call has a compiler counting its arguments before
tasks 13 and 22 change how many there are.
**Pointers:** `web/.svelte-kit/tsconfig.json` (generated by `svelte-kit sync`; its
`exclude` array lists `../src/service-worker.ts` alongside the `.js`, `.d.ts` and
`src/service-worker/**` spellings, and that array is why the file is invisible).
`web/tsconfig.json` (extends it, and is the only project `pnpm check:ci` runs
today). `web/package.json:11` (`check`) and `:12` (`check:ci`), both
`svelte-kit sync && tsc --noEmit -p tsconfig.json`. `web/src/service-worker.ts:11`
(`$service-worker`, typed in `.svelte-kit/ambient.d.ts`), `:16` (the `feed_page`
import, **the only one in the repo**), `:19` (`mortar_wasm_bg.wasm?url`), `:260`
(the positional call). `justfile` `test` recipe (`cd web && pnpm check:ci`) and
`.github/workflows/ci.yml` (`just wasm`, then `just check`).

## Steps

- [ ] Add `web/tsconfig.worker.json`, verified against the tree as it stands:
      `"extends": "./tsconfig.json"`,
      `"compilerOptions": { "lib": ["esnext", "webworker"] }`,
      `"include": [".svelte-kit/ambient.d.ts", ".svelte-kit/non-ambient.d.ts", "src/service-worker.ts"]`,
      `"exclude": []`. The empty `exclude` is the whole mechanism: it overrides the
      generated array that drops the file. The `lib` override is not decoration,
      a worker has no `document` and no `window`, so this project is stricter than
      the app's rather than a second copy of it.
- [ ] Comment the new file with why it exists and what it cannot see, in mason's
      voice: it counts arguments, it cannot read their order.
- [ ] Change `check` and `check:ci` in `web/package.json` to run both projects:
      `svelte-kit sync && tsc --noEmit -p tsconfig.json && tsc --noEmit -p tsconfig.worker.json`.
- [ ] Prove the lane bites, once, by hand: pass a fifth argument to
      `feed_page(actor, cursor, mode, intent)` at `service-worker.ts:260`, run
      `cd web && pnpm check:ci`, confirm it fails on the arity, and revert.
- [ ] Update `.specs/10-build-release-deploy.md:54`'s `just test` row to say two
      tsc projects, the app and the service worker, rather than one.

## Definition of done

- [ ] `cd web && pnpm exec tsc -p tsconfig.worker.json --listFiles` lists
      `src/service-worker.ts` **and** `src/lib/mortar-wasm/pkg/mortar_wasm.d.ts`.
      Listing the first without the second would mean the call resolved to `any`
      and the project proves nothing.
- [ ] Both projects are green with **no edit to any source file**. This task is
      two config files and one spec row; a source fix appearing in the diff means
      the second project was configured differently from the one the plan
      verified, and the difference has to be explained.
- [ ] The by-hand arity break fails `pnpm check:ci` and the failure names
      `feed_page`. Recorded in the PR body, reverted in the diff.
- [ ] The PR states the limit plainly: this lane catches an **arity or type**
      mismatch on `feed_page`, and it cannot catch a **transposition**, because
      after task 22 the call is six `Option<String>` slots in a row and swapping
      two of them typechecks. Tasks 13 and 22 each carry the Playwright case that
      covers the order.
- [ ] The PR also states the one new failure mode: `web/src/lib/mortar-wasm/pkg/`
      is gitignored and `just check` does not run `just wasm`, so on a tree that
      has never built the wasm, `pnpm check:ci` now fails to resolve the module
      where before it silently skipped the file. CI is unaffected (`ci.yml` runs
      `just wasm` first) and so are `just dev`, `just build`, `just test-e2e` and
      `just deploy`, which all depend on `wasm`. The remedy is `just wasm`, and
      the error message says which module is missing.
- [ ] `cd web && pnpm knip` and `just check` are both still green: knip reads
      `tsconfig.json` and oxfmt formats only `src`, so neither should notice, and
      "should" is not evidence.
- [ ] Meets the repo definition of done (`just check` green, no em dash, a *why*
      comment on the new config file). No changeset: nothing user-visible changed.
- [ ] Reviewable: `cd web && pnpm exec tsc -p tsconfig.worker.json --listFiles | grep service-worker`
      prints the file, and the same command against `tsconfig.json` prints
      nothing. That pair is the one-line proof of what this task bought.

## Open questions

- The second project covers the whole of `service-worker.ts`, not just the
  `feed_page` call: the IndexedDB code, the shell cache and the fetch handler
  come with it. That is verified green today, so it costs nothing now, but it
  does mean a future edit to any of that file can fail the gate for a reason
  unrelated to the feed. That is the intended trade and it is recorded here
  rather than discovered later.
