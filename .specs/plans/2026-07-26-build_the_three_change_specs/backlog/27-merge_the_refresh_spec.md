# Task 27 · merge the refresh spec and reconcile

**Plan:** [plan.md](../plan.md) · **Certificate:** [27-merge_the_refresh_spec-certificate.md](27-merge_the_refresh_spec-certificate.md)

**Implements:** [`changes/2026-07-26-refresh_the_wall.md`](../../../changes/2026-07-26-refresh_the_wall.md) §Merge plan and every one of its `Proposed changes` blocks, plus the one-time cross-spec reconciliation all three merges share.
**Depends on:** 19, 26
**Produces:** all three change specs merged, the goals numbered once, and `.specs/README.md` with an empty pending list.
**Pointers:** `.specs/01-domain-model.md:152` (the Snapshot section), `:156`-`:157` (the identity prose the `01` block replaces), `:159` ("State it owns:") and `:161`-`:174` (the state table, which lists exactly `Inner`'s fields; the shipped `refresh` is a `pub` field on `Snapshot` itself, which that table does not cover). `.specs/02-feed-engine.md:32`, `.specs/04-sources-and-moderation.md:279`, `.specs/05-caching-and-persistence.md:64`, `.specs/06-wire-contract.md:24`, `.specs/07-web-client.md:132`, `.specs/08-wall-and-bricks.md:206`, `canonical-types.schema.json`. `.specs/README.md:47` carries the sentence "Three change specs are pending", which only this task can correct truthfully.

## Steps

- [ ] Apply every `Proposed changes` block to its canonical page and bump each edited page's `**Date:**` to the merge date. **Three of those blocks are not applications, they are three-way merges**, because the tree they were drafted against is not the tree they land on; they are enumerated in the merge step below, and applying them verbatim silently reverts task 19. A fourth, `05`'s cache bullet, is a plain application whose *truth* depends on the table it lands on, and it is checked in the same step.
- [ ] Confirm the `refresh` row lands somewhere honest in `01`: not in the `Inner` state table at `:161`-`:174`, but with `Snapshot`'s own immutable identity fields at `:156`-`:157`, saying plainly it is not guarded state. The block already reads that way (it opens "**Not** a row in the state table"), so this is a placement check rather than a rewrite.
- [ ] Add `FeedRefresh` to `canonical-types.schema.json` beside `FeedMode` and `FeedIntent`, with the description pointing at `refresh_from_query` in `server/crates/mortar-core/src/feed.rs`.
- [ ] Write what shipped, not what was proposed, in four places: the `07` state-machine block reflects however task 25 resolved the freeze race on the one-shot flag; the `08` Refreshing paragraph gains **one sentence its block does not carry**, saying what a reduced-motion refresh is (`FeedGrid.svelte:184` freezes the instant `warming` flips true, with no scroll event at all, so a reduced-motion refresh is a cursorless preview that carries the flag **plus** a cursorless freeze that does not, held to one flagged request by task 25's in-flight guard: one refreshed fan-out, and a wall that never reflows because the superseded preview's page is discarded. Read what task 25 and task 26 actually shipped rather than copying this sentence: describing a reflow those readers never see, or a single request where there are two, are both divergences this bullet exists to prevent). The rest of that paragraph is applied, not authored: the block already states the order (`refresh()` first, the scroll second and only when not already at the top) and the live region (the wall keeps its single polite one, a refresh is a warm, `RefreshWall` adds none), and task 26 shipped both, so confirm rather than reopen. Then `02`'s Refresh section says the flag rides on snapshot **creation**, so a refresh that collides with an existing snapshot inherits that snapshot's flag; and the reader close is described where it shipped, at the control in `RefreshWall`, not in `FeedState`.
- [ ] Merge the three blocks that collide with what already landed, rather than pasting them, and check the fourth against the table it lands on. The three are places where the refresh spec was drafted against the branch as it was **before** the feed spec landed, so the refresh spec's version is missing `FeedTarget`:
      - **`02-feed-engine.md` → Entry point.** The refresh spec prints `handle_feed(state, actor: &str, cursor, mode, intent, refresh: bool)`. The canonical page already carries task 19's `handle_feed(state, target: FeedTarget<'_>, cursor, mode, intent)`. The shipped signature, and the one that must end up on the page, is `handle_feed(state, target: FeedTarget<'_>, cursor, mode, intent, refresh: bool)`. Pasting the refresh block reverts `FeedTarget` out of the canonical page and leaves `02` describing an engine that has not existed since task 13.
      - **`06-wire-contract.md` → What the fixture covers.** The refresh spec's block is a single row, `query.mode` / `query.intent` / `query.refresh`. The page already carries task 19's `query.mode` / `query.intent` / `query.target` **plus** a `vocab.hiddenLabels` row. The merged table carries `query.mode` / `query.intent` / `query.target` / `query.refresh` and keeps `vocab.hiddenLabels`. Pasting drops `target` and can drop `hiddenLabels` with it.
      - **`07-web-client.md` → The feed state machine.** The refresh spec's block says "drop this (actor, mode) from the session cache". Task 15 changed `#key` to take a target, so the sentence is "(target, mode)".
      - **`05-caching-and-persistence.md` → The caches** (the one to check rather than merge). The block states **no count**: it says a refresh bypasses two of these on a graph wall and one on a feed wall, names `author_feed`, `image_feed` and `feed_pages`, and closes with "Every other cache stays warm". That is deliberate, and it is what makes it applicable verbatim to a table that grew a twelfth row at task 12. So the obligation is not arithmetic, it is truth: `feed_pages` exists on the page above it, task 22 made a refresh bypass it on a feed wall, and the sentence must still describe that after the paste. Do not reintroduce a number, here or in `02`; a count is what goes stale the next time the table grows.
- [ ] Add the feed-wall clause to `02`: on a feed wall `refresh` bypasses the `feed_pages` entry rather than the author-feed caches, which task 22 built. It must agree word for word with the `05` sentence applied above; they are the same rule stated on two pages.
- [ ] Flip the change spec's `**Status:**` to `Merged`, add `**Merged:**`, move it to `.specs/changes/merged/`, and update `.specs/README.md`'s two tables.
- [ ] Reconcile the two lane descriptions, which only this task can do truthfully because it is the only one that runs after all four Playwright specs exist. `.specs/07-web-client.md:264`'s Playwright row says the lane covers "the worker intercepts /api/feed and lays the demo wall", and `.specs/10-build-release-deploy.md:55`'s `just test-e2e` row says "the Playwright service-worker smoke". By here `web/tests/` holds four specs: the smoke, the reader (task 06), the feed picker (task 18) and the refresh (task 26), plus the positional-slot cases tasks 13 and 22 added to the smoke. Both rows say so, and the `07` row keeps its point that this is the only lane that can render a component.
- [ ] Reconcile once, for all three: correct the sentence at `.specs/README.md:47`, confirm `00-overview.md`'s goals are numbered contiguously after tasks 07 and 19 both added to them, and confirm each merged spec's "what it owes the other two" note is either discharged or annotated as discharged. Four obligations, four tasks: the `App.PageState` interface to 01 and 17, the overlay exclusion rule to 17, the `feed_pages` bypass to 22, and closing an open reader on refresh to **26**, at the control, because `reader.svelte.ts` imports `feed.svelte.ts` and the reverse would be a cycle.
- [ ] Add a minor changeset.

## Definition of done

- [ ] Every `Proposed changes` block is applied and every edited page's `**Date:**` is the merge date.
- [ ] The three collisions are merged, not pasted: `02-feed-engine.md` §Entry point reads `handle_feed(state, target: FeedTarget<'_>, cursor, mode, intent, refresh: bool)`; `06-wire-contract.md` §What the fixture covers carries `query.mode` / `query.intent` / `query.target` / `query.refresh` **and** still carries the `vocab.hiddenLabels` row; and `07-web-client.md`'s state-machine block says (target, mode). `05-caching-and-persistence.md` §The caches, an application rather than a merge, names all three bypassed caches with the wall each belongs to. Checked by reading all four against the code, not against either change spec.
- [ ] The `05` sentence and the `02` clause state **no count**, and both are true of the table above them: `author_feed` and `image_feed` on a graph wall, `feed_pages` on a feed wall, every other cache warm. A number reintroduced during the merge is a defect rather than a nicety: the block deliberately carries none, which is why it survived the table growing a twelfth row at task 12 and will survive the next one.
- [ ] Both lane descriptions name four Playwright specs rather than one smoke: `07-web-client.md` §Testing's Playwright row and `10-build-release-deploy.md:55`'s `just test-e2e` row. `ls web/tests/*.test.ts` is the check.
- [ ] `.specs/README.md` lists no pending change specs, its merged table carries all three of the 2026-07-26 batch, and the sentence at `:47` no longer claims three are pending.
- [ ] `00-overview.md`'s goals are contiguously numbered and each of the three additions is present exactly once.
- [ ] All three files exist under `.specs/changes/merged/` and none remains in `.specs/changes/`.
- [ ] Meets the repo definition of done (`just guard-dashes`, `just check` and `just test-e2e` green; a changeset exists).
- [ ] Reviewable: `ls .specs/changes/*.md` returns nothing, and `rg -n 'refresh_the_wall|lay_a_bluesky_feed|read_a_brick_in_place' .specs/README.md` shows each name only in the merged table.
